{
  "name": "api-analyser-mail-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Email relay for API Analyser — one Vercel function that sends report emails through Resend, so a self-hosted install never has to hold a Resend API key.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "test": "bun test tests",
    "test:watch": "bun test tests --watch"
  },
  "dependencies": {
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "resend": "^6.20.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/bun": "^1.2.0",
    "@types/node": "^22.10.5",
    "@types/react": "^19.0.2",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.19.0"
  }
}
